const initialQuotesState = {
    quotes: [],
};

const quotesReducer = (store = initialQuotesState, action) => {
    switch (action.type) {
        // Define quote-related actions
        default:
            return store;
    }
};

export default quotesReducer;